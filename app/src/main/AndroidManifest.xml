<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.komshuu.komshuuandroidfrontend">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ApartmentActivity"
            android:label="@string/apartment"></activity>
        <activity
            android:name=".EmergencyNumbersActivity"
            android:label="@string/emergency_number"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/homepage"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".UserOrderActivity"
            android:label="@string/order"></activity>
        <activity
            android:name=".FlatActivity"
            android:label="@string/flat"></activity>
        <activity android:name=".KomshuuActivity"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ComplaintActivity"
            android:label="@string/complaints"></activity>
    </application>

</manifest>